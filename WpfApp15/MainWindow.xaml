<Window x:Class="WpfApp15.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp15"
        mc:Ignorable="d"
        Title="MainWindow" Height="323" Width="658">
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Название товара" Width="200" Margin="0,0,0,10" />
            <TextBox x:Name="name" Text="{Binding ProductName}" Width="200" Margin="0,0,0,10" />
            <TextBlock Text="Стоимость" Width="200" Margin="0,0,0,10" />
            <TextBox x:Name="price" Text="{Binding ProductPrice}" Width="200" Margin="0,0,0,10" />
            <Button Content="Добавить товар" 
                    Command="{Binding AddProductCommand}" />
            <ListBox ItemsSource="{Binding Products}" Margin="0,10,0,0" Height="300">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" Width="150" />
                            <TextBlock Text="{Binding Price}" Width="50" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

        </StackPanel>
    </Grid>

</Window>
